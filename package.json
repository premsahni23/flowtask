{
  "name": "flowtask",
  "version": "2.0.0",
  "description": "AI-powered Kanban SaaS with Firebase auth and real-time sync",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
